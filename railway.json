{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "./gradlew bootJar"
  },
  "deploy": {
    "startCommand": "java -Xms128m -Xmx256m -XX:MaxMetaspaceSize=128m -jar build/libs/*.jar",
    "restartPolicyType": "ON_FAILURE"
  }
}
